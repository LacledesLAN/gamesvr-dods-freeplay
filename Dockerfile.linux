# escape=`

FROM lacledeslan/gamesvr-srcds-dods:linux

LABEL maintainer "Laclede's LAN <contact@lacledeslan.com>"

ENV IMAGE="gamesvr-srcds-dods-freeplay:linux"

USER root

HEALTHCHECK NONE

# UPDATE USERNAME
RUN usermod -l DODSFreeplay DODS

USER DODSFreeplay

COPY ./dist /app/bin/

COPY ./dist.linux /app/bin/dod/
	
WORKDIR /app/bin/
